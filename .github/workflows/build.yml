name: SpaghettiOS

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-24.04
    container:
      image: debian:bookworm-slim
    timeout-minutes: 360

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          apt-get update -qq
          apt-get install -y -qq \
            live-build \
            debootstrap \
            debian-archive-keyring \
            xorriso \
            isolinux \
            syslinux-utils \
            grub-pc-bin \
            grub-efi-amd64-bin \
            mtools \
            squashfs-tools \
            cpio \
            make \
            ca-certificates

      - name: Build ISO
        run: bash scripts/build-iso.sh
        env:
          DEBIAN_FRONTEND: noninteractive

      - name: Upload ISO
        uses: actions/upload-artifact@v4
        with:
          name: SpaghettiOS-amd64
          path: build/iso/SpaghettiOS-*.iso
          if-no-files-found: error
          retention-days: 14
          compression-level: 0
